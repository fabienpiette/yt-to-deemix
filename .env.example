# Deemix instance URL (required)
DEEMIX_URL=http://localhost:6595

# Deezer ARL token for authentication (required)
# Get yours from: browser cookies on deezer.com (cookie name: "arl")
DEEMIX_ARL=

# Web server port (optional, default: 8080)
PORT=8080

# Navidrome / Subsonic integration (optional)
# All three must be set to enable skip-if-exists feature
NAVIDROME_URL=
NAVIDROME_USER=
NAVIDROME_PASSWORD=
# Match mode: "substring" (default), "exact", or "fuzzy" (Levenshtein similarity >= 80%)
NAVIDROME_MATCH_MODE=substring
